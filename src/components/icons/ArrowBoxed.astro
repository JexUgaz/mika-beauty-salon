---
import ArrowBoxedClientIcon from "@/components/client/icons/ArrowBoxedClientIcon";

interface Props {
  className: string;
}

const { className } = Astro.props;
---

<ArrowBoxedClientIcon className={className} />
